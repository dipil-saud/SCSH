 			<% form_remote_tag(:url => {:action => 'update_data', :subject => params[:subject], :predicate => params[:predicate], :object => params[:object], :objtype => params[:objtype]},
                         :update => 'details',
                         :html => {}) do %>
	      <% if params[:type] == nil %>
          <%= text_field_tag :value %>
        <% else %>
          <%= select_tag :value, options_for_select([""]) +options_for_select(@uris.collect{ |u| [@names[@uris.index(u)] , u] })%>
        <% end %>
			  <%= submit_tag 'Update' %>  
			<% end -%>
			
